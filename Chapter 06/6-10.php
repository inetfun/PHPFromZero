<?php
//程序名称:6-10.php
//程序功能: break与for和if的应用
 
 	//设定变量 
	 $a=1;
	 $sum=0;
	 //增加变量$a的值 
	 for($a=1;;$a++)
{
 	 //计算变量$sum的值 
 	 $sum+=$a;
 	 //判断变量的值是否大于1000 
	if($sum>1000)
			//如果变量的值大于1000就跳出循环 
			break;
}
 	//输出变量$sum大于1000时变量$a的值 
 	echo '$a=',$a;
 	//输出跳出循环变量$sum的值 
	echo '$sum=',$sum;
?>